Browser calculator based of JS Math methods
- Normal calculator with limited functions
Functions:
- Addition
- Subtraction
- Multiplication 
- Division
- Percentage

Try to replicate the image above (native iOS calculator). 

Don't set the browser to identify buttons for specific functions. Group certain buttons based on a similar function (like base numbers). Add inline functions to other buttons.

For buttons that contain a writable character (display), clicking on them would just display whatever is contained in its 'innerHTML'.

Next category of buttons would be those with dual-functions, such as the brackets and the negative sign modifier. 

Final category would be the functional buttons, such as the equator and the clear.